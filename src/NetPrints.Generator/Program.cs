namespace NetPrints.Generator;

/// <summary>
/// Entry point of the <c>NetPrints.Sdk</c> build-time generator: reads a request file (written by
/// the SDK's MSBuild targets) and writes the <c>.netpc.g.cs</c> next to each graph document
/// (project-system.md §3, added in T044).
/// </summary>
internal static class Program
{
    private static int Main(string[] args)
    {
        return 0;
    }
}
